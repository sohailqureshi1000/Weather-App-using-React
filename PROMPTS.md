# Prompts Used During Development

1. "Build a similar React application independently using AI as a development assistant" — asked to be guided from scratch since I had zero React experience.

2. Asked to explain core React concepts (Component, JSX, Props, State, useEffect) with simple examples before starting.

3. "Weather App (city search, API-based)" — chose the app idea.

4. Requested step-by-step setup instructions for creating a React project with Vite.

5. Asked how to open the project folder in VS Code after creating it via terminal.

6. Requested initial App.jsx code with a city input field and state to track typed text.

7. Requested App.jsx code to fetch weather data from a free API (no key required) using the Open-Meteo API — geocoding city name to lat/lon, then fetching current weather.

8. Requested CSS styling for a clean, dark-themed, centered card layout.

9. Requested UX improvements: loading state on the search button, disabling the button while loading, and allowing search via the Enter key in addition to the button click.

10. Requested help creating PROMPTS.md and README.md for the assignment submission.